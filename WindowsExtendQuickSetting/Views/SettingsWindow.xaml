<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WindowsEthernetControl.Views.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="设置 / Settings">
    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="设置 / Settings" FontSize="24" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Spacing="12">
            <Border Background="#FF333333" CornerRadius="8" Padding="14,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="开机自启动" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="Start with Windows" FontSize="11" Foreground="#AAAAAA"/>
                    </StackPanel>
                    <ToggleSwitch x:Name="StartToggle" Grid.Column="1" VerticalAlignment="Center"
                                  Toggled="StartToggle_Toggled"/>
                </Grid>
            </Border>

            <Border Background="#FF333333" CornerRadius="8" Padding="14,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="语言 / Language" FontSize="14" FontWeight="SemiBold"
                               Foreground="White" VerticalAlignment="Center"/>
                    <ComboBox x:Name="LangCombo" Grid.Column="1" Width="120" VerticalAlignment="Center"
                              SelectionChanged="LangCombo_SelectionChanged">
                        <ComboBoxItem Content="中文" IsSelected="True"/>
                        <ComboBoxItem Content="English"/>
                    </ComboBox>
                </Grid>
            </Border>

            <Border Background="#FF333333" CornerRadius="8" Padding="14,10">
                <StackPanel>
                    <TextBlock Text="关于 / About" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Text="Windows Ethernet Control v1.0" FontSize="12" Foreground="#AAAAAA" Margin="0,4,0,0"/>
                    <TextBlock Text="有线网卡控制工具" FontSize="12" Foreground="#AAAAAA"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Window>
